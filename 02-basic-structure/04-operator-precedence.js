let result = 12 - 6 / 3
let result2 = (3 + 4) * 2

let isGreater = 20 - 6 * 3 >= 4
let isGreater2 = 20 - 6 * 3 >= 1
//precedence   3    13  14  11

console.log('12 - 6 / 3 = ', result)
console.log('(3 + 4) * 2 = ', result2)

console.log('20 - 6 * 3 >= 4 -> ', isGreater)
console.log('20 - 6 * 3 >= 1 -> ', isGreater2)

/**
 * Operator precedence
 * Operator precedence determines how operators are parsed
 * concerning each other. Operators with higher precedence
 * become the operands of operators with lower precedence.
 *
 * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Operator_Precedence
 */
